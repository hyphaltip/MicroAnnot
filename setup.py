"""
Minimal setup.py for editable installs (pip install -e .) and legacy tooling.
All package metadata lives in pyproject.toml.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
